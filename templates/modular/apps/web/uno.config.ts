import { defineConfig } from 'unocss'
import { sharedPresets, sharedRules, sharedShortcuts } from '@monohouse/uno-config'

export default defineConfig({
  presets: sharedPresets,
  rules: sharedRules,
  shortcuts: sharedShortcuts,
})
